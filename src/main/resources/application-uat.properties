spring.jpa.database=POSTGRESQL
spring.datasource.driver-class-name=${DB_DRIVER:org.postgresql.Driver}
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/ems_test}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:1234}
spring.jpa.properties.hibernate.dialect=${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.show-sql=${DB_SHOW_SQL:true}
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}

##JWT SECURITY
security.token.access.secret-key=${JWT_ACCESS_SECRET_KEY:2023BillantedChangeAccessKey}
security.token.access.expiry-length=${JWT_ACCESS_EXPIRY_LENGTH:180000000}
security.token.refresh.secret-key=${JWT_REFRESH_SECRET_KEY:2023BillantedChangeRefreshKey}
security.token.refresh.expiry-length=${JWT_REFRESH_EXPIRY_LENGTH:43200000}

##CORS CONFIG
cors.allowed-credential=${CORS_ALLOWED_CREDENTIALS:true}
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
cors.allowed-headers=${CORS_ALLOWED_HEADERS:Origin, X-Requested-With, Content-Type, Accept, Authorization}
cors.allowed-methods=${CORS_ALLOWED_METHODS:GET, POST, PUT, DELETE, OPTIONS}

##PAYSTACK CONFIGURATION
paystack.secret-key=${PAYSTACK_SECRET_KEY:sk_test_e44b040732630cb4a313d4087b5ef8352e252ae0}
paystack.initialize-payment-url=${PAYSTACK_INITIALIZE_PAYMENT_URL:https://api.paystack.co/transaction/initialize}
paystack.call-back-url=${PAYSTACK_CALLBACK_URL:http://localhost:3000/my-dashboard/}
paystack.verification-url=${PAYSTACK_VERIFICATION_URL:https://api.paystack.co/transaction/verify/}

spring.jackson.deserialization.fail-on-unknown-properties=${JACKSON_FAIL_ON_UNKNOWN_PROPERTIES:true}

# Twilio Configuration
twilio.account-sid=${TWILIO_ACCOUNT_SID:1234567890}
twilio.auth-token=${TWILIO_AUTH_TOKEN:0987654321}
twilio.phone-number=${TWILIO_PHONE_NUMBER:08167144768}

# Email Configuration
spring.mail.host=${EMAIL_HOST:smtp.gmail.com}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USERNAME:cymarck@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:1234}
spring.mail.properties.mail.smtp.auth=${EMAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${EMAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.transport.protocol=${EMAIL_TRANSPORT_PROTOCOL:smtp}

# OTP Templates
app.otp.sms.template=${OTP_SMS_TEMPLATE:Your Cymark verification code is: %s. Valid for 10 minutes.}
app.otp.email.subject=${OTP_EMAIL_SUBJECT:Cymark - Verification Code: %s}
app.otp.email.template=${OTP_EMAIL_TEMPLATE:<!DOCTYPE html><html><head><style>body{font-family:Arial,sans-serif;line-height:1.6}.container{max-width:600px;margin:0 auto;padding:20px}.otp-code{font-size:24px;font-weight:bold;color:#2563eb}.footer{margin-top:20px;padding-top:20px;border-top:1px solid #eee}</style></head><body><div class=\"container\"><h2>Your Verification Code</h2><p>Your OTP code is: <span class=\"otp-code\">%s</span></p><p>This code is valid for 10 minutes.</p><p>If you didn't request this code, please ignore this email.</p><div class=\"footer\"><p>Best regards,<br>Cymark Team</p></div></div></body></html>}

# SpringDoc OpenAPI Configuration
springdoc.api-docs.path=${SWAGGER_API_DOCS_PATH:/v3/api-docs}
springdoc.swagger-ui.path=${SWAGGER_UI_PATH:/swagger-ui.html}
springdoc.swagger-ui.operations-sorter=${SWAGGER_OPERATIONS_SORTER:method}
springdoc.swagger-ui.tags-sorter=${SWAGGER_TAGS_SORTER:alpha}
springdoc.swagger-ui.enabled=${SWAGGER_UI_ENABLED:true}
springdoc.api-docs.enabled=${SWAGGER_API_DOCS_ENABLED:true}

# Customize Swagger UI
springdoc.swagger-ui.doc-expansion=${SWAGGER_DOC_EXPANSION:none}
springdoc.swagger-ui.filter=${SWAGGER_FILTER:true}